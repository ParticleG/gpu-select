post_install() {
    echo "==> NOTE: gpu-select requires switcheroo-control.service to detect GPUs."
    echo "==> Run: sudo systemctl enable --now switcheroo-control.service"
}

post_upgrade() {
    post_install
}
